# Landing Page Overrides

> **PROJECT:** joan-kernelsu
> **Page Type:** Device-specific kernel landing

Rules here override `design-system/joan-kernelsu/MASTER.md`.

## Layout

- Signature element: LG V30 second-screen **ticker** (scrolling `joan · H930 · msm8998 · …`).
- Hero is **asymmetric**: copy left, 18:9 phone chassis with defconfig dump on the right.
- Facts as a 4-cell spec strip, not three equal cards.
- No testimonials. No A/B color theater.

## Copy

- Device first (`joan` / H930), then ROM nightly, then hook mode.
- Primary CTA: Download boot image. Secondary: View source.
- Flash path must say **fastbootd** (`adb reboot fastboot`).
- Footer: “This site is vibecoded. The kernel is not.”
